import styled from "styled-components";

export const DamageIndicatorWrapper = styled.div`
	--arrow-margin: 2px;
	--arrow-size: calc(var(--wrapper-height) / 2 - var(--arrow-margin) * 2);

	display: flex;
`;
